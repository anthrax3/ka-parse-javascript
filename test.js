function apply_tests(text) {
    /**
     * text :: Javascript string
     * return :: { passes: boolean,
     *             message: string }
     */
    

    return has(text, "Program");
    // TODO
}
